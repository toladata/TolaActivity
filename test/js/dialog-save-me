browser.url('https://tola-activity-demo.mercycorps.org/indicators/home/0/0/0/');
      let buttons = $('#toplevel_div').$$('div.panel-body');
      let targetDiv = link.getAttribute('data-target');
      let table = $('div' + targetDiv).$('table');
let buttons = $('#toplevel_div').$$('div.panel-body');
button = buttons[0];
let link = button.$('a');
link.click();
link.click();
link.getAttribute('data-target');
targetDiv = link.getAttribute('data-target');
let targetDiv = link.getAttribute('data-target');
targetDiv = link.getAttribute('data-target');
targetDiv
let target = link.getAttribute('data-target');
target
table = $('div'+target).$('table');
$('div'+target).$('table');
let table=$('div'+target).$('table');
table=$('div'+target).$('table');
button
button.getText();
$('div' + target).$('table');
let t=$('div' + target).$('table');
tr=t.$$('a#indicator-link');
tr=t.$$('a.indicator-link');
tr=t.$$('tbody>tr>td>a.indicator-link');
tr=t.$$('tbody>tr>td>a.indicator-link');
row=tr[1];
row.getText();
rowtext=row.getText();
row.click();
row.click();
row.click();
row.click();
assert(browser.isVisible('div#indicator_modal_div'));
rowtext
$('div#indicator_modal_header>h3').getText();
$('div#indicator_modal_header>h3').getText().split(':')[1].trim();
dialogtext = $('div#indicator_modal_header>h3').getText().split(':')[1].trim();
assert.equal(rowtext, dialogtext);
